import 'package:flutter/material.dart';

class PickInterestPage extends StatelessWidget {
  const PickInterestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
